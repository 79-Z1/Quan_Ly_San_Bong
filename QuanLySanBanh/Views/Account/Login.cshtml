@*@model QuanLySanBanh.Models.TaiKhoan
@{
    ViewBag.Title = "Login";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="~/Content/dangNhap.css">
    <title>Đăng nhập</title>
</head>
<body>
    <section>
        <div class="form-box">
            <div class="form-value">
                @using (Html.BeginForm("CheckLogin", "TaiKhoans", FormMethod.Post))
                {
                @Html.AntiForgeryToken()
                <h2>Đăng nhập</h2>
                <div class="inputbox">
                    <ion-icon name="person-outline"></ion-icon>
                    @Html.TextBoxFor(m => m.Email, new { type = "email", required = "required" })
                    <label for="">Email</label>
                </div>
                <div class="inputbox">
                    <ion-icon name="lock-closed-outline"></ion-icon>
                    @Html.PasswordFor(m => m.MatKhau, new { required = "required" })
                    <label for="">Mật khẩu</label>
                </div>
                <div class="forget">
                    <label for=""><input type="checkbox">Nhớ mật khẩu  <a href="#">Quên mật khẩu</a></label>
                </div>
                <button type="submit">Đăng nhập</button>
                <div class="register">
                    <p>Chưa có tài khoản</p>
                    <a href="/TaiKhoans/Register">Đăng ký</a>
                </div>
                }
            </div>
        </div>
    </section>
    <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>
</body>
</html>*@